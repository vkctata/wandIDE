import React,{useState} from 'react'; import {createRoot} from 'react-dom/client'; import {Activity, Bell, Bot, CheckCircle2, ChevronDown, Clock3, Code2, FolderGit2, GitPullRequest, Github, Hash, LayoutDashboard, MessageSquare, Play, Plus, Search, Settings, Sparkles, TerminalSquare, TimerReset, Users, Zap} from 'lucide-react'; import './styles.css';

type View='home'|'threads'|'tasks'|'notifications'|'settings';
const repos=[{name:'forgepad',path:'~/Code/forgepad',color:'#89b4fa',count:4},{name:'api-gateway',path:'~/Code/api-gateway',color:'#cba6f7',count:2},{name:'design-system',path:'~/Code/design-system',color:'#f9e2af',count:1}];
const events=[{kind:'PR review',repo:'forgepad',title:'Improve agent run timeline',meta:'Maya Chen · 12m ago',tag:'needs review',tone:'purple',icon:GitPullRequest},{kind:'Agent completed',repo:'api-gateway',title:'Weekly dependency audit',meta:'Sentinel · 34m ago',tag:'passed',tone:'green',icon:CheckCircle2},{kind:'New thread',repo:'design-system',title:'Should tokens live in a separate package?',meta:'Ravi Patel · 1h ago',tag:'discussion',tone:'yellow',icon:MessageSquare},{kind:'PR comment',repo:'forgepad',title:'Can we make this state recoverable?',meta:'Jon Bell · 2h ago',tag:'3 replies',tone:'blue',icon:MessageSquare}];
function App(){const [view,setView]=useState<View>('home'); const [repo,setRepo]=useState(repos[0]); const [query,setQuery]=useState(''); const nav=(v:View)=><button className={view===v?'nav active':'nav'} onClick={()=>setView(v)}>{v==='home'?<LayoutDashboard/>:v==='threads'?<MessageSquare/>:v==='tasks'?<Clock3/>:v==='notifications'?<Bell/>:<Settings/>}<span>{v[0].toUpperCase()+v.slice(1)}</span>{v==='notifications'&&<b className="badge">3</b>}</button>; return <div className="app"><aside><div className="brand"><div className="mark"><Sparkles size={17}/></div><span>forgepad</span><small>private beta</small></div><div className="workspace"><div className="avatar">VC</div><div><strong>Venkata's space</strong><small>Personal workspace</small></div><ChevronDown size={15}/></div><div className="navgroup"><label>Workspace</label>{nav('home')}{nav('threads')}{nav('tasks')}{nav('notifications')}</div><div className="navgroup repos"><label>Repositories <Plus size={13}/></label>{repos.map(r=><button key={r.name} className={'repo '+(repo.name===r.name?'selected':'')} onClick={()=>{setRepo(r);setView('threads')}}><i style={{background:r.color}}/><span>{r.name}</span><em>{r.count}</em></button>)}</div><div className="bottom">{nav('settings')}<div className="status"><span/> All systems operational</div></div></aside><main><header><div className="crumb"><span>Workspace</span><b>/</b><strong>{view==='home'?'Overview':view==='threads'?repo.name:view[0].toUpperCase()+view.slice(1)}</strong></div><div className="actions"><div className="search"><Search size={15}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search anything"/><kbd>⌘ K</kbd></div><button className="iconbtn"><Bell size={17}/><i/></button><div className="avatar">VC</div></div></header>{view==='home'?<Home setView={setView}/>:view==='threads'?<Threads repo={repo}/>:view==='tasks'?<Tasks/>:view==='notifications'?<Notifications/>:<SettingsView/>}</main></div>}
function Home({setView}:{setView:(v:View)=>void}){return <section className="content"><div className="hero"><div><p className="eyebrow"><span className="pulse"/> MONDAY, AUGUST 8, 2026</p><h1>Good morning, Venkata.</h1><p className="sub">Your agents have been busy while you were away.</p></div><button className="primary" onClick={()=>setView('tasks')}><Plus size={16}/> New task</button></div><div className="stats"><Stat icon={Zap} value="12" label="Agent runs today" hint="↑ 18% vs last week"/><Stat icon={GitPullRequest} value="4" label="PRs need your eyes" hint="Across 2 repositories"/><Stat icon={TimerReset} value="2h 48m" label="Time saved" hint="By your agents this week"/></div><div className="sectionhead"><div><h2>Activity</h2><p>Everything happening across your workspace.</p></div><button className="filter">All activity <ChevronDown size={14}/></button></div><div className="timeline">{events.map((e,i)=><article className="event" key={i}><div className={'eventicon '+e.tone}><e.icon size={17}/></div><div className="eventbody"><div className="eventtop"><span className="kind">{e.kind}</span><span className={'tag '+e.tone}>{e.tag}</span><span className="time">{e.meta.split(' · ')[1]}</span></div><h3>{e.title}</h3><p><span className="repo-dot"/> {e.repo} <b>·</b> {e.meta.split(' · ')[0]}</p></div><button className="more">•••</button></article>)}</div><div className="sectionhead agents"><div><h2>Active agents</h2><p>Specialists working across your repositories.</p></div><button className="textbtn" onClick={()=>setView('settings')}>Manage agents <span>→</span></button></div><div className="agentgrid"><Agent icon={Bot} name="Code reviewer" desc="Reviews new pull requests" status="Watching 3 repos"/><Agent icon={TerminalSquare} name="Sentinel" desc="Dependency & security audits" status="Next run · Tomorrow 09:00"/><Agent icon={Code2} name="Pair programmer" desc="Your on-demand coding partner" status="Ready when you are"/></div></section>}
function Stat({icon:Icon,value,label,hint}:{icon:any,value:string,label:string,hint:string}){return <div className="stat"><Icon size={18}/><strong>{value}</strong><span>{label}</span><small>{hint}</small></div>} function Agent({icon:Icon,name,desc,status}:{icon:any,name:string,desc:string,status:string}){return <div className="agent"><div className="agenticon"><Icon size={18}/></div><div><h3>{name}</h3><p>{desc}</p><small><span className="green-dot"/> {status}</small></div><button>•••</button></div>}
function Threads({repo}:{repo:any}){return <section className="content"><div className="hero compact"><div><p className="eyebrow"><FolderGit2 size={14}/> REPOSITORY</p><h1>{repo.name}</h1><p className="sub">Threads and agent context for {repo.path}</p></div><button className="primary"><Plus size={16}/> New thread</button></div><div className="threadlist">{['Make the deploy preview recoverable','Improve agent run timeline','Release checklist · v2.4.0','Pair on auth middleware'].map((x,i)=><div className="thread"><div className="threadicon"><Hash size={16}/></div><div><h3>{x}</h3><p>{i%2?'Maya Chen':'Code reviewer'} · {i+1} {i===0?'hour':'days'} ago</p></div><span className="tag blue">{i+1} replies</span><ChevronDown size={16}/></div>)}</div></section>}
function Tasks(){return <section className="content"><div className="hero compact"><div><p className="eyebrow"><Clock3 size={14}/> AUTOMATIONS</p><h1>Scheduled tasks</h1><p className="sub">Let your agents handle recurring engineering work.</p></div><button className="primary"><Plus size={16}/> Schedule task</button></div><div className="taskcard"><div className="taskicon"><Zap size={17}/></div><div><h3>Weekly dependency audit</h3><p>Sentinel · api-gateway</p></div><code>0 9 * * 1</code><span className="tag green">Active</span><button className="run"><Play size={14}/> Run now</button></div><div className="emptyhint"><Sparkles size={20}/><h3>Build your agent routine</h3><p>Schedule reviews, tests, changelog drafts, or any CLI workflow with a familiar cron expression.</p></div></section>}
function Notifications(){return <section className="content"><div className="hero compact"><div><p className="eyebrow"><Bell size={14}/> INBOX</p><h1>Notifications</h1><p className="sub">PR comments and agent events that need your attention.</p></div><button className="textbtn">Mark all read</button></div><div className="notice"><div className="eventicon purple"><MessageSquare size={17}/></div><div><b>Jon Bell commented on Improve agent run timeline</b><p>“Can we make this state recoverable?” · forgepad</p></div><span>2h</span></div><div className="notice"><div className="eventicon blue"><GitPullRequest size={17}/></div><div><b>PR #184 is ready for review</b><p>Improve agent run timeline · forgepad</p></div><span>3h</span></div></section>}
function SettingsView(){return <section className="content"><div className="hero compact"><div><p className="eyebrow"><Settings size={14}/> PREFERENCES</p><h1>Settings</h1><p className="sub">Configure your workspace, integrations, and agents.</p></div></div><div className="settingscard"><h2>Connected providers</h2><p>Credentials stay encrypted in your local keychain.</p><div className="provider"><Github size={19}/><div><b>GitHub</b><small>Personal access token · Connected</small></div><button className="outline">Manage</button></div><div className="provider"><GitPullRequest size={19}/><div><b>Azure DevOps</b><small>Personal access token · Not connected</small></div><button className="outline">Connect</button></div><h2 className="space">Repository folder</h2><div className="folder"><FolderGit2 size={18}/><span>~/Code</span><button className="outline">Choose folder</button></div></div></section>}
createRoot(document.getElementById('root')!).render(<App/>);
